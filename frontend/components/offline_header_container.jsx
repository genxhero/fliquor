import {connect} from 'react-redux';
import OfflineHeader from './offline_header';

const mapStateToProps = state => ({

});

const mapDispatchToProps = dispatch => ({

});

export default connect(mapStateToProps, mapDispatchToProps)(OfflineHeader);
